# Event contracts

CloudEvents envelope handling is owned by `github.com/cloudevents/sdk-go/v2` in the Go implementation. This directory contains only domain payload schemas. It does not define a custom CloudEvents envelope.
