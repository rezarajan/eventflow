apiVersion: v1
kind: ConfigMap
metadata:
  name: eventflow-config
data:
  resources.yaml: |
    apiVersion: eventflow.dev/v1alpha1
    kind: SQLiteJournal
    metadata:
      name: gateway-journal
    spec:
      path: /var/lib/eventflow/journal.sqlite
